@{
    Html.DroppableZone("AfterMain").Title("After Main").Render();
    Html.DroppableZone("AfterMainRecursive").Title("After Main Recursive").Render();
    var site = Html.DroppableZone("AfterMainSite");
    if (!Html.IsSiteZoneEditable())
    {
        site.SetState(ControlPanelState.Visible);
    }
    site.Title("After Main Site").Render();
}